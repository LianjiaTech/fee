// 执行 mocha ../dist/test
var assert = require('assert')
/*
describe('#test_demo()', function () {
  it('2 > 1 么？', function () {
    assert.equal(true, 2 > 1) // pass
  })
  it('2 < 1 么?', function () {
    assert.equal(true, 2 < 1) // fail
  })

  it('2 > 3 么？', function () {
    assert.equal(true, 2 > 3) // fail
  })
  it('2 < 3 么?', function () {
    assert.equal(true, 2 < 3) // pass
  })
})

*/
